'use strict';
const Validation=(()=>{const rules={population:v=>v>0||'validationPopulation',percentagePopulationUsingService:v=>v>=1&&v<=4||'validationPopulationUsing',averageFare:v=>v>0||'validationFare',commission:v=>v>=0&&v<=50||'validationCommission',rideGrowth:v=>v>=-90&&v<=200||'validationGrowth',marketingBudget:v=>v>=0||'validationMarketing',initialInvestment:v=>v>=0||'validationInvestment',operatingExpenses:v=>v>=0||'validationExpenses'};function validateAssumptions(a){const errors={};Object.keys(rules).forEach(k=>{const val=Number(a[k]);const res=rules[k](val);if(res!==true||Number.isNaN(val))errors[k]=Localization.t(res===true?'validationFare':res)});return{valid:Object.keys(errors).length===0,errors}}return{validateAssumptions}})();
