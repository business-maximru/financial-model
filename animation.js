'use strict';
const Animation=(()=>{const prev={};function animateNumber(el,key,value,formatter){const start=prev[key]??value;const end=value;prev[key]=value;const t0=performance.now();const dur=450;function frame(t){const p=Math.min(1,(t-t0)/dur);const eased=1-Math.pow(1-p,3);el.innerHTML=formatter(start+(end-start)*eased);if(p<1)requestAnimationFrame(frame)}requestAnimationFrame(frame)}function flashCells(){document.querySelectorAll('[data-recalc]').forEach(el=>{el.classList.remove('flash');void el.offsetWidth;el.classList.add('flash')})}return{animateNumber,flashCells}})();
